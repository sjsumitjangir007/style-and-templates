{{ define "index/index.tmpl" }}
<!DOCTYPE html>
<html>
<head>
  {{template "layout/layout_metatags" .}}
  {{template "layout/layout_head_scripts" .}}
</head>
<body>
    <div class="root flex">
        {{template "layout/layout_aside" .}}

    <div class="h-screen flex flex-col w-full">
        {{template "layout/layout_header" .}}
        <section id="main-section" class="p-4 w-full h-full dark:bg-gray-700 overflow-auto bg-blue-50/50" >
            {{template "cards/template" .}}
        </section>
        {{template "layout/layout_footer" .}}
    </div>
    </div>

</body>
</html>
{{ end }}